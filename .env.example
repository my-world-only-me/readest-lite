# ───────────────────────────────────────────────────────────────────────────
# Readest Lite — 环境变量样例
# 复制为 .env 并按需修改。完整说明见 https://cshdotcom.github.io/readestl/deploy.html
# ───────────────────────────────────────────────────────────────────────────

# ── 必填：管理员账号 ───────────────────────────────────────────────────────
# 首次启动自动创建管理员账号
# 管理员可在用户中心创建/管理普通用户，设置配额
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-password
# 可选：管理员显示名（v8.1.0 新增）。用户列表里展示用。未设置时 displayName 为空，UI 回退到邮箱
# ADMIN_USERNAME=Admin

# ── 可选：对外端口 ────────────────────────────────────────────────────────
# 默认 8225。docker-compose.yml 中端口映射为 ${PORT:-8225}:8225
PORT=8225

# ── 可选：JWT 密钥 ────────────────────────────────────────────────────────
# 不设置时由 ADMIN_EMAIL + ADMIN_PASSWORD 派生（开箱即用，但生产环境建议显式设置）
# 生成命令：openssl rand -hex 32
# JWT_SECRET=

# ── 可选：DeepL 翻译（默认关闭，设置 API key 后自动开启）───────────────
# DEEPL_ENABLED=false  # 设为 true 启用翻译功能
# DEEPL_FREE_API_KEYS=  # DeepL Free API key（逗号分隔多个）
# DEEPL_PRO_API_KEYS=   # DeepL Pro API key（逗号分隔多个）

# ── 可选：AI 聊天网关（不设则 AI 功能不可用）─────────────────────────────
# AI_GATEWAY_API_KEY=

# ── 可选：对外访问 URL（反向代理场景下必填）──────────────────────────────
# 用于生成上传/下载文件的签名 URL。未设置时默认 http://localhost:8225
# 如果通过反向代理对外提供服务（如 https://read.yourdomain.com），必须设置为该外部地址，
# 否则签名 URL 指向 localhost，浏览器外不可达
# PUBLIC_BASE_URL=https://read.yourdomain.com
